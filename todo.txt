# 全分散式 Java 语义抽离混淆器 TodoList

## 架构和混淆规则
- 架构规则：混淆后的代码绝不能使用统一入口点、Core或中央调度器。所有功能必须分散到独立的单一用途的代理类中。
- 代码生成规则：混淆后的业务逻辑类只允许包含数字常量、变量赋值以及对分散的静态代理类的调用。必须严格避免字符串字面量、'new' 关键字、原始名称、控制流关键字(if/for/while/switch)和真实的API调用。
- 命名规范规则：生成的混淆代理类必须具有完全动态的名称（例如，随机生成的有效Java标识符，如UUID变体），没有任何硬编码字符串、可预测的前缀或固定格式。

## 1. 基础架构搭建
- [x] 初始化项目目录结构
- [x] 确定并引入核心处理依赖 (针对JAR/DEX的解析与重写)
- [x] 编写基础的输入输出处理框架骨架

## 2. 核心混淆引擎开发 (语义抽离提取器)
- [x] 字符串提取与代理类生成器
  - [x] 编写 StringProxyGenerator: 生成包含 Base64 或加密字符串还原逻辑的单文件 Java 代码
- [x] 类实例化提取与代理类生成器
  - [x] 编写 ClassCreationProxyGenerator: 生成利用反射机制(Class.forName(name).newInstance())实例化对象的 Java 代码
- [x] 方法调用提取与代理类生成器
  - [x] 编写 MethodProxyGenerator: 生成根据方法名/签名利用反射调用方法的 Java 代码
- [x] 字段读写提取与代理类生成器
  - [x] 编写 FieldProxyGenerator: 生成利用反射读写特定字段的 Java 代码
- [x] 控制流提取与代理类生成器
  - [x] 编写 ControlFlowProxyGenerator: 生成处理判断条件的 Java 代码
- [x] 原始业务逻辑重写引擎 (全部替换为代理类和ID调用)
  - [x] 构建替换规则引擎骨架(ProxyManager)
  - [x] 完善替换逻辑，处理复杂的加载、运算指令映射

## 3. 不同文件格式的解析与处理实现
- [x] 实现对 .jar 文件的解析与混淆重写 (字节码层面 ASM)
  - [x] 实现 InMemoryCompiler，使用 javax.tools.JavaCompiler 动态编译代理类生成的 Java 源码
  - [x] 实现 ProxyManager，统筹代理类的生成、记录和名字生成，保证混淆代理类只生成一次
  - [x] 使用 ASM 实现基本的 ClassReader 和 ClassWriter 框架，读写 JAR 中的 .class 文件
  - [x] 编写 ClassVisitor 和 MethodVisitor 拦截常量池、方法指令（当前重点支持 LDC 字符串拦截）
  - [x] 将原始逻辑替换为对新生成的动态命名代理类的调用（如静态方法 `get()`）
  - [x] 将 InMemoryCompiler 编译生成的代理类 .class 文件注入到输出 JAR 包中
  - [x] 实现 MethodVisitor 拦截 INVOKE 类指令替换为代理调用
  - [x] 实现 MethodVisitor 拦截 NEW 指令替换为代理调用
  - [x] 实现 MethodVisitor 拦截 FIELD 读写指令替换为代理调用
  - [x] 实现 MethodVisitor 拦截 控制流/比较 指令替换为代理调用 (完善针对各类跳转指令如 IFEQ, IFNULL, IF_ICMPEQ 等的处理)
- [ ] 实现对 .dex 文件的解析与混淆重写 (Dexlib2/Smali)
  - [x] 评估并引入 smali/dexlib2 依赖
  - [x] 创建 DexCompiler 接口抽象 .class 到 .dex 的编译过程
  - [x] 在 DexEngine 中使用 dexlib2 加载并重新写入原始 .dex 文件
  - [x] 遍历 dexlib2 解析出的 Classes 和 Methods 结构
  - [x] 在 Method 指令中拦截字符串字面量 (如 const-string) 并输出日志
  - [x] 提取并重写 Dex 文件结构中的字符串数据段并替换为代理调用
  - [x] 提取并重写 Dex 文件中的静态字段访问指令 (拦截 sget/sget-object)
  - [x] 提取并重写 Dex 文件中的实例字段访问指令 (拦截 iget/iput/sput)
    - [x] 实现 IGET 和 IGET_OBJECT 指令拦截与代理调用生成
    - [x] 研发能够正确拦截基本类型写入 (IPUT/SPUT) 并进行精确类型推导而不触发 Dalvik VerifyError 的新方案
  - [x] 提取并重写 Dex 文件中的方法调用指令 (拦截 invoke-virtual, invoke-static 等) 需解决参数装箱与堆栈问题
    - [x] 设计 Dex 方法调用栈帧分析器，以准确计算寄存器偏移和装箱机制
    - [x] 编写 Dex MethodProxyGenerator，专门生成兼容 Dex 语法的反射调用桩代码
  - [ ] 提取并重写 Dex 文件中的对象实例化指令 (拦截 new-instance 和 invoke-direct <init>) 需处理指令组合 (注: 由于 Dalvik/ART VerifyError 限制，暂缓在 DEX 层面直接拦截 NEW_INSTANCE)
    - [ ] 通过代码流分析(Data Flow Analysis)查找 `new-instance` 和对应 `<init>` 调用之间的寄存器关联
      - [ ] 优化实例化代理逻辑，防止复杂对象实例化时抛出 `InstantiationException`
      - [ ] 支持对多维数组类型 (`new-array`, `filled-new-array`) 的拦截与代理封装
  - [ ] 提取并重写 Dex 文件中的控制流指令 (拦截 if-eq/if-ne等) 需处理跳转偏移修正 (注: 由于 Dalvik VerifyError，暂跳过 Dex 中的 IF_* 指令拦截)
    - [ ] 将所有 `if-*` 转换为 `if-nez` 调用动态生成的代理方法，处理 `goto` 跳转表的自动修正
      - [ ] 为 Dexlib2 引入 Control Flow Graph (CFG) 以解决复杂循环与多级跳转条件代理时的死循环或栈溢出问题
      - [ ] 处理 `packed-switch` 和 `sparse-switch` 指令的重写逻辑，将其混淆并替换为自定义路由方法
  - [x] 将动态生成的 Java 代理类使用 d8/dx API 编译为 .dex 格式，并合并到目标 DEX 文件中
  - [x] 支持多 DEX (Multi-Dex) 文件的处理：解析所有 classesN.dex，并统筹注入代理类，确保不超过65535方法数限制
    - [x] 引入 `DexPool` 和 `DexMerger` 进行多 DEX 解析与重整
    - [x] 实现跨 Dex 文件的代理类共享或冗余分发机制
  - [x] 支持 APK 文件的解包、签名和重新打包 (集成 apktool / apksigner / zipalign)
    - [x] 使用 ZipInputStream 和 ZipOutputStream 自动解压 APK 并提取 `classes.dex`
    - [ ] 解析和修改 AndroidManifest.xml (如添加/替换 Application 类以执行解密或反调试)
    - [x] 集成 `apksigner` Java API (或者命令行调用) 对重打包后的 APK 进行 V1/V2 签名
    - [x] 对打包后的 APK 进行 `zipalign` 4字节对齐处理
      - [ ] 添加对 APK 内部资源文件 (例如 `res/`，`assets/`) 的结构化解析与内容遍历，预留资源混淆和隐写接口
      - [x] 研究如何重新压缩对齐前的 `.apk` 包体，使其体积最小化，并对无用文件进行自动剔除 (如 `META-INF/` 旧签名)

## 4. 混淆类随机化与加固
- [x] 代理类内部逻辑多样化
  - [x] 随机挑选异或算法或多重 Base64 算法生成还原方法 (StringProxyGenerator)
  - [x] 为其他代理类引入逻辑随机化 (ClassCreation, Method, Field, ControlFlow)
- [x] 反射与 MethodHandle 混合使用
  - [x] 在代理类中随机使用传统 Reflection 或 JDK 7+ MethodHandle 调用目标方法 (MethodProxy, FieldProxy, ClassCreationProxy)
- [ ] 无效死代码与花指令生成
  - [x] 在生成的控制流代理类 (ControlFlowProxy) 内插入对运行结果无影响的复杂算术运算和条件分支
  - [x] 将花指令扩展到主类的原始方法中 (在 ObfuscationMethodVisitor 中插入 NOP, goto, 等无用跳转与算术指令)
  - [x] 增强花指令，插入使用不透明谓词跳过的无效异常抛出和非法指令流
  - [x] 在 DexEngine 中也实现针对 DEX 字节码的指令膨胀和花指令插入
  - [x] 实现针对方法级别的基本块打乱 (Block splitting and shuffling)
    - [x] 构建 Control Flow Graph (CFG) 以识别和分割 Basic Blocks (基础版实现于 ObfuscationMethodVisitor.shuffleBlocks)
    - [x] 将块使用 `GOTO` 打乱顺序重新写入指令流
- [x] 深度字符串加密
  - [x] 支持通过动态密钥解密原始字符串，密钥可以分散在多个代理类中
    - [x] 设计基于时间的密钥轮换或基于堆栈状态的动态解密种子生成器 (通过调用栈哈希值动态还原 AES Key)
  - [x] 引入 AES / DES / RC4 等加密算法对常量池字符串进行静态加密，动态解密
  - [x] 实现自定义混淆字典，进一步加固生成的标识符名称（例如 OOO000 风格或完全不可见字符风格）
  - [ ] 动态加载密钥：从外部服务器、JNI库或环境隐写获取解密密钥
    - [ ] 设计环境变量隐写和资源文件隐写机制 (Steganography) 存储密钥片段
  - [ ] 基于网络动态下发解密密钥，实现特定环境授权后才可执行逻辑
  - [ ] 探究硬件指纹绑定 (Hardware ID Binding)，使得混淆代码只能在特定设备或CPU架构上正确执行

## 5. 高级混淆特性与保护
- [x] Opaque Predicates (不透明谓词) 插入
  - [x] 生成总是为真或为假的复杂条件表达式，增加反编译器的分析难度
  - [x] 实现基于数学定理的谓词，使得静态分析难以求解 (扩展不透明谓词库，增加更多的数学恒等式，如 (x^3 - x) % 3 == 0)
  - [ ] 实现基于状态机的动态谓词生成
    - [ ] 注入全局 State 变量，结合方法调用图实时更新，在关键判定点引入依赖 State 的复杂跳转
- [x] 控制流平坦化 (Control Flow Flattening)
  - [x] 将方法内原有的基本块转化为一个由大的 switch-case 语句管理的调度器循环 (针对简单非分支方法实现真实平坦化)
  - [ ] 增加多级跳板，使得 switch 逻辑更加复杂
  - [x] 动态计算 switch 状态转移值，防范静态常量推导
    - [x] 引入非线性方程或多变量组合作为 `switch` `case` 跳转值的计算依据 (如 state = (state ^ key) + (state % offset))

- [x] 虚拟机保护 (VM Protection) (初步实现)
  - [x] 编写 `VirtualMachine` 类，实现基于字节数组的栈式虚拟机解释器
  - [x] 定义自定义指令集，包含入栈、加减法、返回等基本指令
  - [x] 在 `ObfuscationMethodVisitor` 中拦截简单算术方法，将其转化为自定义字节码并调用虚拟机执行
  - [x] 增强 `VirtualMachine` 虚拟机保护，增加局部变量指令 (OP_LOAD, OP_STORE) 和 控制流跳转指令 (OP_JMP, OP_IFEQ, OP_IFNE, OP_IF_ICMPEQ 等等)。
  - [x] 增强 `ObfuscationMethodVisitor` 虚拟化重写逻辑，引入 Two-pass 解析以支持带局部变量和条件分支跳转的复杂方法。
  - [x] 完善 VirtualMachine 和虚拟化混淆 (Virtualization Obfuscation) 新增栈操作指令 (OP_DUP, OP_POP, OP_SWAP) 和数组操作指令 (OP_IALOAD, OP_IASTORE, OP_ARRAYLENGTH)
  - [x] 增强控制流扁平化 (Control Flow Flattening) 引入多级跳板 (Trampolines) 和伪造的 case 分支
  - [x] 增强花指令 (Flower Instructions / Opaque Predicates) 扩展 insertOpaquePredicate 库，新增基于费马小定理 (x^7 - x) % 7 == 0 等数学恒等式的不透明谓词
  - [x] 增强指令替换 (Instruction Substitution) 为 IADD 增加新的数学等价替换策略：a + b = (a ^ b) + ((a & b) << 1)
  - [x] 增强数字混淆 (Number Obfuscation) 增加基于字符串长度 String.length() 动态推导整数常量的策略
- [x] 异常控制流
  - [x] 使用 try-catch-finally 块代替正常的逻辑跳转，使用 throw 代替 goto 或 return
    - [x] 在 `ObfuscationMethodVisitor` 中安全分析栈帧(Stack Map Frames)，在方法末尾的 Return 区块使用 ATHROW 抛出带有返回值的自定义异常对象
  - [x] 植入虚假异常处理逻辑，扰乱控制流分析
    - [x] 伪造冗余的 `ExceptionTable` 并指向无效但合法的代码块
- [ ] 反调试与反运行环境检测
  - [x] 注入静态防调试逻辑代码 (Anti-Debugging Injector)
    - [x] 编写 AntiDebugGenerator 生成包含 `android.os.Debug.isDebuggerConnected()` 的代理类
    - [x] 在 `JarBytecodeEngine` 及 `DexEngine` 中，寻找入口类 (`main`, `Activity.onCreate` 或 `<clinit>`) 并将反调试桩代码织入其中
  - [ ] 注入检测代码，识别是否在模拟器、Xposed 环境
    - [ ] 检测特定的 `ClassLoader` (如 `de.robv.android.xposed.XposedBridge`，`Lcom/saurik/substrate/MS$2;`)
    - [ ] 检查特定的系统属性特征，如 qemu 硬件指纹 `ro.hardware.qemu`，`ro.product.model` 包含 emulator 字样
  - [ ] 检测 Root 环境以及 Frida/Xposed 框架 Hook
    - [ ] 扫描特定的端口 (如 Frida Server 的 27042 端口) 并检测 `su` 二进制文件
    - [ ] 读取 `/proc/self/maps` 并检测是否映射了可疑的 `.so` (如 `frida-agent.so`)
  - [ ] 加入自毁机制或应用层重启逻辑响应安全威胁
    - [ ] 当检测到调试或篡改时，使用 `Runtime.getRuntime().halt(0)` 强杀进程或制造内存溢出
    - [ ] 设计抛出 `VirtualMachineError` 等难以通过普通 `catch` 捕获的严重错误
- [ ] 反编译工具抵抗 (Anti-Decompiler)
  - [x] 构造引发 CFR / Procyon / Jadx 等主流反编译工具崩溃的特殊指令序列 (如非法的局部变量表/异常表)
  - [x] 利用 Java 语言规范与 Class 文件规范差异，进行类库漏洞攻击混淆
    - [x] 插入重叠的 Try-Catch 块 (Overlapping Try-Catch) 让 Jadx 无法生成结构化的 Java AST
- [x] 指令替换 (Instruction Substitution) 增强
  - [x] 引入更多的指令等价替换规则，如 `IMUL` (a * b -> (a + a + ... b times)), `INEG` (-a -> ~a + 1), `IXOR` (a ^ b -> (a | b) - (a & b))。
- [x] 数字混淆 (Number Obfuscation) 增强
  - [x] 扩充 `obfuscateNumber()` 的策略，结合多种数学运算（加减、异或、位移等）将数字常量分解为运行时的动态计算式。

## 6. 测试验证模块
- [x] 针对各类代理类的边界测试
  - [x] 测试代理类名生成器在高并发下是否有冲突
  - [x] 确保混淆对基本数据类型的处理安全无误
    - [x] 添加包含 `byte`, `short`, `double`, `long` 等全类型混合运算的单元测试用例
  - [x] 加入对带有多重泛型的类方法调用的代理测试
    - [x] 验证泛型擦除后反射调用的方法签名匹配准确性
- [ ] 混淆鲁棒性测试
  - [ ] 集成到真实的大型 Spring Boot 和 Android 项目中进行端到端构建和混淆
  - [ ] 分析混淆后应用的内存和 CPU 占用率变化，优化代理类的类加载性能

## 7. 高级特性预研与长期规划 (未来迭代)
- [ ] 脱壳与 Hook 对抗机制落地
  - [ ] 增加针对 eBPF/Kprobe 的底层 Hook 检测机制
  - [ ] 基于 ptrace 的反动态注入防护：创建一个子进程使用 ptrace 附加主进程 (ptrace(PTRACE_TRACEME, 0, 0, 0)) 以阻止其他调试器附加
- [ ] 动态控制流混淆技术进阶
  - [x] 虚假异常注入预研: 设计在正常执行路径中抛出并捕获自定义异常机制，迫使反编译器分析庞大而无用的异常表
  - [ ] 控制流平坦化(Control Flow Flattening)深入研究: 实现类似 OLLVM 的 `switch-case` 状态机分发器
  - [ ] 基于分支跳转隐写术 (Branch obfuscation via steganography)
    - [ ] 将控制流逻辑隐藏在特定的图片像素或预定义文件的 Byte 序列中
- [ ] 动态指令生成技术与运行时环境保护
  - [ ] 预研指令自修改(Self-modifying code)的可行性，在 Dalvik/ART 内存加载后动态解密真正代码字节
    - [ ] 使用 `Unsafe` 机制或 JNI 拦截 `DefineClass` 来运行时修改已被加载类的字节码
  - [ ] 环境感知的变异: 检测运行时设备类型，针对真实机器与模拟器执行不同的代理分支逻辑以增加对抗成本
  - [ ] 蜜罐和陷阱设计(Honeypots): 构建诱导动态分析工具进行 Hook 的伪装方法，并设置自毁或报错陷阱
    - [ ] 生成包含诱饵名称的方法 (如 `verifyLicense`, `getSecretKey`) 诱导攻击者进行 Hook 并触发警报
- [ ] 针对安卓生态特性与构建系统的保护增强
  - [ ] Android 资源文件(AAR, XML, Assets)全链路混淆、压缩及加密
    - [ ] 修改 `resources.arsc` 将资源名混淆为不可见字符
  - [ ] JNI 层面的原生代码联动 (JNI Obfuscation): 将核心解密和控制流逻辑抽离至 C/C++ SO 层并进行 VMP (Virtual Machine Protect) 处理
  - [ ] 构建 Gradle 插件: 开发自动挂载到 `Transform` 或 `Task` 层的集成插件，对开发者提供无感知的无缝混淆体验
- [ ] 脱壳与 Hook 对抗机制落地
  - [ ] 多维度检测内存中的非正常执行环境 (例如检测 `ro.debuggable`, `/data/local/tmp` 等敏感路径)
  - [ ] 基于 Xposed, Frida, Magisk 等常见注入框架与内核态 Hook 行为的检测、绕过与拦截阻断
    - [ ] 使用 In-line Hook 技术对抗安全软件和分析框架的 Got 表劫持
  - [ ] 动态反调试(Anti-Debugging)增强，结合 ptrace 检测与双进程互相看守(Daemon Process)机制实现对 JDWP 的全面防护

## 8. 文档与生态维护
- [ ] 完善项目的技术架构图、数据流图与执行时序图
  - [ ] 新增多层混淆代理调用栈的时序图 (Call Sequence Diagram) 和数据脱敏流转图 (Data Flow Chart)
  - [ ] 绘制 DEX 重组打包与 D8 合并引擎的工作流概览
- [ ] 编写详细的开发者接入使用说明与常见构建/运行时报错排查指南(Troubleshooting)
  - [ ] 针对高版本 Android (`API 28+`) 反射限制导致的 `NoSuchMethodError` 及 Bypass 方式编写指南
  - [ ] 撰写常见 Dalvik `VerifyError` (如堆栈帧不匹配，寄存器重用冲突) 的案例分析及解决方案
- [ ] 自动构建及持续集成(CI/CD)脚本编写 (GitHub Actions 或 GitLab CI) 并且配置代码质量与测试覆盖率卡点
  - [ ] 构建每日夜间安全回归测试 (Nightly Regression Testing)，用最新的 jadx / enjarify 分析混淆效果
  - [ ] 配置 Dependabot 自动化升级 dexlib2, ASM 和 D8 等核心依赖版本
- [ ] 构建在线体验与测试平台: 提供网页端直接上传小文件(Jar/Dex)并返回混淆结果的沙盒系统
  - [ ] 实现前端展示混淆前后 AST 对比和流程图 (Control Flow Graph visualization)

- [x] 编写测试用例验证代理类的生成与运行正确性
  - [x] 为 StringProxyGenerator 编写单元测试
  - [x] 为 ClassCreationProxyGenerator 编写单元测试
  - [x] 为 MethodProxyGenerator / FieldProxyGenerator 编写单元测试
  - [x] 为 JarBytecodeEngine / ObfuscationClassVisitor 编写基础测试 (LDC测试)
  - [x] 为 JarBytecodeEngine 编写更全面的测试 (包括 Method, Class Creation, Field 拦截)
- [x] 完善 DexEngine 测试用例
  - [x] 验证 DEX 中 const-string 提取和代理类生成正确性
  - [x] 验证 DEX 中 sget / sget-object 指令能够被完全移除并替换为代理调用
  - [x] 验证包含 invoke-virtual 和 new-instance 的 DEX 文件能被正确修改
  - [x] 验证通过 DexEngine 生成的文件可以使用 DexFileFactory 正确读取
  - [x] 验证 DexEngine 在处理 APK 时能够正确剔除旧签名并进行 zipalign 重排
- [ ] 针对真实复杂应用 (Spring Boot, Android App) 进行混淆与反编译效果评估
  - [ ] 准备一组基准测试应用 (如开源商城App、开源博客后端)，编写混淆对比评测脚本
  - [ ] 使用 CFR, Procyon, Fernflower, Jadx 等常见反编译器测试被混淆代码的抗性，并解析反编译器崩溃日志
  - [ ] 测试被混淆的应用的运行时性能损耗与内存占用增加情况，使用 JMH 框架量化代理调用的纳秒级延迟
  - [ ] 提供混淆前后的包大小对比与内存分析报告
  - [ ] 自动化度量指标计算：统计方法中基本块分裂的碎片率、代理类占总类的比例等，可视化输出图表

## 9. 高并发与性能调优
- [ ] 代理类生成引擎的线程安全与并发优化
  - [x] 针对 ProxyManager 进行多线程并发瓶颈分析，引入分段锁 (Segmented Locks) 或无锁并发数据结构
    - [x] 使用 ConcurrentHashMap 替换 HashMap 进行代理类映射缓存管理
    - [x] 引入 computeIfAbsent 原子操作代替 putIfAbsent，避免冗余生成和编译
  - [ ] 将 ASM 的方法访问 (MethodVisitor) 与类访问 (ClassVisitor) 的处理逻辑进行并行化改造
    - [ ] 分析 ClassVisitor 和 MethodVisitor 的状态依赖关系，确保并行访问时不会相互干扰
    - [ ] 探索使用 java.util.concurrent.ForkJoinPool 配合并行 Stream 进行 ClassNode 或 MethodNode 层级的批量异步混淆
  - [ ] 并发任务分发器 (Task Dispatcher)：针对海量类文件的项目，使用 `ForkJoinPool` 进行多文件多核并行混淆
    - [ ] 实现针对 Multi-Dex 拆分或者大规模 Jar 包的目录层级/文件层级并发处理框架
    - [ ] 添加并发度动态自适应调节，防止资源占用过高导致 OOM
- [ ] 内存驻留编译器的性能优化 (InMemoryCompiler)
  - [ ] 设计并实现基于缓存的增量编译系统，减少相同/相似代理类的重复编译时间
  - [ ] 为 javax.tools.JavaCompiler 提供异步并发编译队列支持
  - [ ] 在 Android 环境使用时，规避大量反射/代理类的加载开销：探究 `AppCDS` (Application Class-Data Sharing) 及 AOT 编译配合
  - [ ] 开发自定义的基于内存加密的 ClassLoader
- [ ] Dex 合并阶段的优化
  - [ ] 减少 D8 编译 Dex 时对系统内存的开销，实现流式或分批次编译写入
    - [ ] 探索使用 DexIndexedConsumer 结合内存缓冲流以规避全量内存聚合造成的 Full GC
  - [ ] 对 Multi-Dex 分包算法进行优化，尽量使代理类与被代理调用处于同一个 classesN.dex 以缩短运行时链接时间
    - [ ] 构建基本的方法调用依赖图 (Call Graph)，以启发式算法将相关度高的代理类优先分发至原方法所在的 dex 中
  - [ ] 引入 `DexSplitter` 策略，实现按依赖关系和调用频次智能分割方法，使单个 Dex 加载时间最小化
    - [ ] 支持根据混淆方法权重 (如启动链路、热点执行路径) 对 Dex 文件内的方法分布进行静态调整
  - [ ] 实现针对无用类及冗余资源的 DCE (Dead Code Elimination)，在混淆前减小输入包体积

## 10. AI辅助混淆与对抗分析 (前沿探索)
- [ ] 大语言模型 (LLM) 代码逻辑重写与自适应混淆
  - [ ] 实现针对大语言模型反编译的对抗样本生成
  - [ ] 集成离线或在线 LLM 服务，对敏感的单方法业务逻辑进行语义等价的变体生成，使得每次编译产生的 AST 完全不同。
    - [ ] 研究如何限制 LLM 生成的语义等价代码的执行性能损耗在可控阈值内
    - [ ] 构建轻量级 AST 合法性自检工具，快速拦截模型输出的语法或类型错误
  - [ ] 自动生成迷惑性极强的变量名、方法名和日志输出，模拟真实业务代码库结构，混淆逆向人员视听。
    - [ ] 利用 RAG 技术结合 Android/Java API 文档生成更真实的虚假方法名，避免被静态分析特征匹配发现
    - [ ] 结合本地项目的上下文信息微调生成模型，保证虚假方法签名和字符串字面量契合当前工程的业务背景
- [ ] 对抗机器学习静态特征分析
  - [ ] 分析主流反病毒软件 (AV) 和静态检测引擎使用的机器学习特征提取方法 (如 Opcode N-gram, API Call Graph)。
  - [ ] 基于 Transformer 架构的注意力机制特征污染
    - [ ] 持续收集不同反编译/沙箱平台 (如 VirusTotal) 对于混淆前后代码的识别报告进行数据沉淀
  - [ ] 针对性地注入无关且高频的 API 调用、结构化虚假死代码区块，平滑字节码统计特征，以绕过恶意软件检测与沙箱特征分类。
    - [ ] 开发 Opcode 频率均衡器插件，在混淆流最后阶段填平罕见指令频率，隐藏混淆器指纹特征
- [ ] 强化学习引导的控制流混淆策略优化
  - [ ] 构建以对抗反编译器分析耗时和静态分析复杂度为目标的反馈奖励机制。
  - [ ] 训练模型根据特定的输入方法规模与逻辑复杂度，自动组合最适宜的混淆算法集合（如 Opaque Predicates 数量、CFF 深度、花指令密度）。
    - [ ] 定义可量化的混淆强度评价指标体系，提供统一的多目标优化函数 (兼顾混淆效果、体积与性能开销)
